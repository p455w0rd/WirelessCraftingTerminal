buildscript {
	repositories {
		jcenter()
		maven {
			name = "forge"
			url = "http://files.minecraftforge.net/maven"
		}
	}
	dependencies {
		classpath "net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT"
	}
}

apply plugin: "net.minecraftforge.gradle.forge"

version = "1.12.2-" + getVersion("VERSION")
group = "p455w0rd"
archivesBaseName = "WirelessCraftingTerminal"
sourceCompatibility = targetCompatibility = "1.8"

minecraft {
	version = "1.12.2-14.23.0.2546"
	runDir = "run"
	mappings = "snapshot_20171003"
	useDepAts = true
}

repositories {
	mavenCentral()
	maven {
		name = "progwml6"
		url = "http://dvs1.progwml6.com/files/maven"
	}
	maven {
		name = "tterrag"
		url = "http://maven.tterrag.com"
	}
	maven {
		url "http://mobiusstrip.eu/maven"
	}
	maven {
		 name = "epoxide"
		 url = "http://maven.epoxide.xyz"
	 }
	 maven {
		name = "tehnut"
		url = "http://tehnut.info/maven"
	}
}
dependencies {
	//compile ("appeng:appliedenergistics2:rv4-alpha-1:api"){
	//	transitive = false
	//}
	//compile ("appeng:appliedenergistics2:rv4-alpha-7:dev"){
	//	transitive = false
	//}
	deobfCompile "mezz.jei:jei_1.12.2:+"
	//compile project(':appliedenergistics2')
	//deobfCompile "com.enderio:EnderIO:1.10.2-3.0.1.112_beta"
	//deobfCompile "com.enderio.core:EnderCore:1.10.2-0.4.1.55-beta"
	//deobfCompile "mcp.mobius.waila:Waila:1.7.0-B3_1.9.4"
	deobfCompile "net.darkhax.tesla:Tesla-1.12:1.0.61"
	deobfCompile "mcp.mobius.waila:Hwyla:1.8.20-B35_1.12"
	deobfCompile "appeng:appliedenergistics2:rv5-stable-1"
}

task apiJar(type: Jar) {
	classifier = 'api'
	from sourceSets.api.output
	from sourceSets.api.allJava
}

artifacts {
	archives apiJar
}

processResources {
	inputs.property "version", project.version
	inputs.property "mcversion", project.minecraft.version

	from (sourceSets.main.resources.srcDirs) {
		include "mcmod.info"
		expand "version": project.version, "mcversion": project.minecraft.version
	}
		
	from (sourceSets.main.resources.srcDirs) {
		exclude "mcmod.info"
	}
}

String getVersion(String type) {
	String major = "0";
	String revision = "0";
	String patch = "0";
	String prefix = "public static final String $type = \"";
	File file = file("src/main/java/p455w0rd/wct/init/ModGlobals.java")
	file.eachLine { String s ->
		s = s.trim();
		if (s.startsWith(prefix)) {
			s = s.substring(prefix.length(), s.length() - 2);
			String[] pts = s.split("\\.");

			major = pts[0];
			revision = pts[1];
			patch = pts[2];
		}
	}
	return "$major.$revision.$patch";
}
